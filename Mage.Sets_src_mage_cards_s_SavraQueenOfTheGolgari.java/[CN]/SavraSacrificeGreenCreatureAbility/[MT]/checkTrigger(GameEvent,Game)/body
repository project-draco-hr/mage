{
  return event.getPlayerId().equals(this.getControllerId()) && game.getLastKnownInformation(event.getTargetId(),Zone.BATTLEFIELD).getCardType().contains(CardType.CREATURE) && game.getLastKnownInformation(event.getTargetId(),Zone.BATTLEFIELD).getColor(game).isGreen();
}
