{
  try {
    if (isConnected())     return server.getRoomChatId(roomId);
  }
 catch (  MageException ex) {
    handleMageException(ex);
  }
catch (  Throwable t) {
    handleThrowable(t);
  }
  return null;
}
