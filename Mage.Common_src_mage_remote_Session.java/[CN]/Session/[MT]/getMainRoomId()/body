{
  try {
    if (sessionState == SessionState.CONNECTED)     return server.getMainRoomId();
  }
 catch (  RemoteException ex) {
    handleRemoteException(ex);
  }
catch (  MageException ex) {
    handleMageException(ex);
  }
  return null;
}
