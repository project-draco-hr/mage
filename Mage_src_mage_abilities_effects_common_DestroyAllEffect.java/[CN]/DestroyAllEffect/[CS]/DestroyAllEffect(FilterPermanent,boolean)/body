{
  super(Outcome.DestroyPermanent);
  this.filter=filter;
  this.noRegen=noRegen;
  if (noRegen)   staticText="Destroy all " + filter.getMessage() + ". They can't be regenerated";
 else   staticText="Destroy all " + filter.getMessage();
}
