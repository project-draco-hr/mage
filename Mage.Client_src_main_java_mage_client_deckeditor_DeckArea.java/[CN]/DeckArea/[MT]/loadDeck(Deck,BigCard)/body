{
  deckList.loadCards(new CardsView(deck.getCards()),bigCard,null,SortBy.NAME);
  if (sideboardList.isVisible())   sideboardList.loadCards(new CardsView(deck.getSideboard()),bigCard,null,SortBy.NAME);
}
