{
  MageObject object=game.getObject(sourceId);
  if ((object.getCardType().contains(CardType.INSTANT) || object.getAbilities().containsKey(FlashAbility.getInstance().getId()) || game.canPlaySorcery(playerId))) {
    return true;
  }
  return false;
}
