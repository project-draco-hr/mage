{
  if (event.getType() == GameEvent.EventType.DECLARED_ATTACKERS && game.getActivePlayerId().equals(this.getControllerId())) {
    numberOfattackers+=game.getCombat().getAttackers().size();
    if (numberOfattackers > 2) {
      condition=true;
    }
  }
}
