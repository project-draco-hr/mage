{
  String prefix="";
  if (attachmentType == AttachmentType.AURA)   prefix="Enchanted";
 else   if (attachmentType == AttachmentType.EQUIPMENT)   prefix="Equipped";
  staticText=prefix + " creature gains " + ability.getRule();
}
