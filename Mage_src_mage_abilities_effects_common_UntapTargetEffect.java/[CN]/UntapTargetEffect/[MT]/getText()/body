{
  Target target=this.source.getTargets().get(0);
  if (target.getNumberOfTargets() > 1)   return "untap " + target.getNumberOfTargets() + " target "+ this.source.getTargets().get(0).getTargetName()+ "s";
 else   return "untap target " + this.source.getTargets().get(0).getTargetName();
}
