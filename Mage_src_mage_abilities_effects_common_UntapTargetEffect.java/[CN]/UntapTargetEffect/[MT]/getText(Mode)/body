{
  Target target=mode.getTargets().get(0);
  if (target.getNumberOfTargets() > 1)   return "untap " + target.getNumberOfTargets() + " target "+ mode.getTargets().get(0).getTargetName()+ "s";
 else   return "untap target " + mode.getTargets().get(0).getTargetName();
}
