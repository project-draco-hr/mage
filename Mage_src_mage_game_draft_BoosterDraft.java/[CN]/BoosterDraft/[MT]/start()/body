{
  while (boosterNum < sets.size()) {
    openBooster();
    while (!isAbort() && pickCards()) {
      if (boosterNum % 2 == 1) {
        passLeft();
      }
 else {
        passRight();
      }
      fireUpdatePlayersEvent();
    }
  }
  this.fireEndDraftEvent();
}
