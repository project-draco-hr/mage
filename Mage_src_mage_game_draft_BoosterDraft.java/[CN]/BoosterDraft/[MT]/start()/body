{
  while (boosterNum < numberBoosters) {
    openBooster();
    while (!isAbort() && pickCards()) {
      if (boosterNum % 2 == 1) {
        passLeft();
      }
 else {
        passRight();
      }
      fireUpdatePlayersEvent();
    }
  }
  this.fireEndDraftEvent();
}
