{
  long delay=250L;
  int count=5;
  PriorityTimer timer=new PriorityTimer(count,delay,new Runnable(){
    public void run(){
      System.out.println("Exit");
      System.exit(0);
    }
  }
);
  timer.init();
  timer.start();
  Thread.sleep(2000);
  timer.pause();
  Thread.sleep(3000);
  timer.resume();
}
