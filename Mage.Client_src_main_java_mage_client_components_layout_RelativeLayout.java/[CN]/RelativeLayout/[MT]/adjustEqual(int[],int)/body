{
  int pixelsLeft=spaceRemaining;
  for (int i=0; i < relativeSpace.length; i++) {
    if (relativeSpace[i] > 0) {
      relativeSpace[i]++;
      pixelsLeft--;
      if (pixelsLeft == 0) {
        break;
      }
    }
  }
}
