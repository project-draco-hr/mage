{
  Permanent permanent=game.getPermanent(source.getSourceId());
  Player player=game.getPlayer(source.getControllerId());
  if (permanent != null && !permanent.isTapped() && player != null && player.chooseUse(Outcome.DrawCard,"Do you wish to draw a card?",game)) {
    return true;
  }
  return false;
}
