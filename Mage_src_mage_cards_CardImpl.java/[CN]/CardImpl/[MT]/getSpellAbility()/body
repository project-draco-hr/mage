{
  if (spellAbility == null) {
    for (    Ability ability : abilities.getActivatedAbilities(Zone.HAND)) {
      if (ability instanceof SpellAbility)       spellAbility=(SpellAbility)ability;
    }
  }
  return spellAbility;
}
