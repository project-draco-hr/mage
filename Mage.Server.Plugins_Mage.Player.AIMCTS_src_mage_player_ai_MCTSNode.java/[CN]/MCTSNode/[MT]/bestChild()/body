{
  double bestCount=-1;
  MCTSNode bestChild=null;
  for (  MCTSNode node : children) {
    if (node.visits > bestCount) {
      bestChild=node;
      bestCount=node.visits;
    }
  }
  return bestChild;
}
