{
  for (  Permanent permanent : game.getBattlefield().getActivePermanents(new FilterLandPermanent(),source.getControllerId(),game)) {
    if (!permanent.hasSubtype("Island"))     permanent.getSubtype().add("Island");
  }
  return true;
}
