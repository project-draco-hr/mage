{
  super(Outcome.Discard);
  staticText=new StringBuilder("Target opponent reveals his or her hand. You choose ").append(filter.getMessage()).append(" from it. That player discards that card").toString();
  this.filter=filter;
}
