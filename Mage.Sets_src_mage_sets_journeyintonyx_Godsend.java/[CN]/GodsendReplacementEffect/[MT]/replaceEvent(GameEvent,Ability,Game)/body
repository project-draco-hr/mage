{
  Player player=game.getPlayer(event.getPlayerId());
  Permanent sourcePermanent=game.getPermanent(source.getSourceId());
  if (player != null && sourcePermanent != null) {
    game.informPlayer(player,new StringBuilder("You can't cast this spell because a card with the same name is exiled by ").append(sourcePermanent.getName()).append(".").toString());
  }
  return true;
}
