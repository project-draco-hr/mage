{
  replay.start();
  Session session=SessionManager.getInstance().getSession(sessionId);
  if (session != null)   session.fireCallback(new ClientCallback("replayInit",replay.getGame().getId(),new GameView(replay.next(),replay.getGame())));
}
