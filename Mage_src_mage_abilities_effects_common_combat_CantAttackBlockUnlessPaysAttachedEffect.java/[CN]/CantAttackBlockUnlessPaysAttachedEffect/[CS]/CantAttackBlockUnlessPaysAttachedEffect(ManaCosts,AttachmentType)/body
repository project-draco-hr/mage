{
  super(Duration.WhileOnBattlefield,Outcome.Detriment,RestrictType.ATTACK_AND_BLOCK,manaCosts);
  staticText=attachmentType.equals(AttachmentType.AURA) ? "Enchanted " : "Equipped " + "creature can't attack or block unless its controller pays " + manaCosts == null ? "" : manaCosts.getText();
}
