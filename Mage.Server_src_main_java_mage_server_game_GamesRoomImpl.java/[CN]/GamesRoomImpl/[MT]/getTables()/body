{
  ArrayList<TableView> tableList=new ArrayList<TableView>();
  for (  Table table : tables.values()) {
    if (table.getState() != TableState.FINISHED)     tableList.add(new TableView(table));
  }
  return tableList;
}
