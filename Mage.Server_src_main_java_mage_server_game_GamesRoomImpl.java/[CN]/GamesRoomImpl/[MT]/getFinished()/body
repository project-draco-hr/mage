{
  ArrayList<MatchView> matchList=new ArrayList<MatchView>();
  for (  Table table : tables.values()) {
    if (table.getState() == TableState.FINISHED)     matchList.add(new MatchView(table.getMatch()));
  }
  return matchList;
}
