{
  for (int i=0; i < numSeats; i++) {
    if (seats[i].getPlayer().getId().equals(playerId)) {
      seats[i].setPlayer(null);
      if (state == TableState.STARTING)       state=TableState.WAITING;
      break;
    }
  }
}
