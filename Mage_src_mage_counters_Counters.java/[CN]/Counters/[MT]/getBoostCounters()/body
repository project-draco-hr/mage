{
  List<BoostCounter> boosters=new ArrayList<BoostCounter>();
  for (  Counter counter : this.values()) {
    if (counter instanceof BoostCounter)     boosters.add((BoostCounter)counter);
  }
  return boosters;
}
