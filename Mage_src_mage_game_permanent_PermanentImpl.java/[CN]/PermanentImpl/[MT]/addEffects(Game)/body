{
  for (  StaticAbility ability : abilities.getStaticAbilities(Zone.BATTLEFIELD)) {
    for (    Effect effect : ability.getEffects()) {
      if (effect instanceof ContinuousEffect)       game.addEffect((ContinuousEffect)effect,ability);
    }
  }
}
