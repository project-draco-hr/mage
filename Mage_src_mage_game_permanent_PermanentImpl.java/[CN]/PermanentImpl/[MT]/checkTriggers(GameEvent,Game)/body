{
  if (event.getType() == EventType.BEGINNING_PHASE_PRE)   this.controlledFromStartOfTurn=true;
  for (  TriggeredAbility ability : abilities.getTriggeredAbilities(Zone.BATTLEFIELD)) {
    ability.checkTrigger(event,game);
  }
}
