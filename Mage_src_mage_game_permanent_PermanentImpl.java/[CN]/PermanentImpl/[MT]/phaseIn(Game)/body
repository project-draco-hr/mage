{
  if (!phasedIn) {
    if (!replaceEvent(EventType.PHASE_IN,game)) {
      this.phasedIn=true;
      addEffects(game);
      fireEvent(EventType.PHASED_IN,game);
      return true;
    }
  }
  return false;
}
