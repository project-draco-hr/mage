{
  try {
    rmiExecutor.execute(new Runnable(){
      @Override public void run(){
        DraftManager.getInstance().sendCardPick(draftId,sessionId,cardPick);
      }
    }
);
  }
 catch (  Exception ex) {
    handleException(ex);
  }
}
