{
  if (audioManager.playerJoinedTable == null) {
    audioManager.playerJoinedTable=new MageClip(audioManager.loadClip(Constants.BASE_SOUND_PATH + "OnPlayerJoinedTable.wav"),AudioGroup.OtherSounds);
  }
  checkAndPlayClip(getManager().playerJoinedTable);
}
