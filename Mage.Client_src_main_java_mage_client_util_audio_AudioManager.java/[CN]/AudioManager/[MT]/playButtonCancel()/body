{
  if (audioManager.buttonCancelClip == null) {
    audioManager.buttonCancelClip=new MageClip(audioManager.loadClip(Constants.BASE_SOUND_PATH + "OnButtonCancel.wav"),AudioGroup.SkipSounds);
  }
  checkAndPlayClip(getManager().buttonCancelClip);
}
