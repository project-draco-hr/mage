{
  if (audioManager.yourGameStarted == null) {
    audioManager.yourGameStarted=new MageClip(audioManager.loadClip(Constants.BASE_SOUND_PATH + "OnGameStart.wav"),AudioGroup.OtherSounds);
  }
  checkAndPlayClip(getManager().yourGameStarted);
}
