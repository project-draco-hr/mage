{
  if (audioManager.yourGameStarted == null) {
    audioManager.yourGameStarted=new MageClip(audioManager.loadClip(Constants.BASE_SOUND_PATH + "OnYourGameStarted.wav"),AudioGroup.GameSounds);
  }
  checkAndPlayClip(getManager().yourGameStarted);
}
