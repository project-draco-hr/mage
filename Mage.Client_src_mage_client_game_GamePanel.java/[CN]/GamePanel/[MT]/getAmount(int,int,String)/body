{
  MageFrame.getPickNumberDialog().showDialog(min,max,message);
  if (MageFrame.getPickNumberDialog().isCancel())   session.sendPlayerBoolean(gameId,false);
 else   session.sendPlayerInteger(gameId,MageFrame.getPickNumberDialog().getAmount());
}
