{
  int value=life;
  Player opponent=game.getPlayer(game.getOpponents(playerId).get(0));
  if (opponent.getLife() <= 0)   return Integer.MAX_VALUE;
  value-=opponent.getLife();
  for (  Permanent permanent : state.getBattlefield().getAllPermanents()) {
  }
  return value;
}
