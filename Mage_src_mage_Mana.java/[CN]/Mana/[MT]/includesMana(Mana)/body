{
  return green >= mana.green && blue >= mana.blue && white >= mana.white && black >= mana.black && red >= mana.red && (colorless >= mana.colorless || countColored() >= mana.countColored() + mana.colorless);
}
