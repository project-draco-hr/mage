{
  if (type2cards == null) {
    type2cards=new ArrayList<>();
    for (    CardDownloadData data : cards) {
      if (data.isType2() || data.isToken()) {
        type2cards.add(data);
      }
    }
  }
}
