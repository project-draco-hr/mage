{
  if (event.getType() == GameEvent.EventType.DAMAGE_PLAYER && event.getSourceId().equals(source.getSourceId())) {
    DamagePlayerEvent damageEvent=(DamagePlayerEvent)event;
    if (damageEvent.isCombatDamage()) {
      return true;
    }
  }
  return false;
}
