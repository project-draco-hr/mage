{
  boolean result=false;
  if (permanent.moveToZone(Zone.GRAVEYARD,null,this,false)) {
    if (!this.isSimulation()) {
      this.informPlayers(new StringBuilder(permanent.getLogName()).append(" is put into graveyard from battlefield").toString());
    }
    result=true;
  }
  return result;
}
