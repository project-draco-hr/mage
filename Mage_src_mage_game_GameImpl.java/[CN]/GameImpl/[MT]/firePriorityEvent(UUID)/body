{
  String message=this.getStep().toString();
  if (this.getActivePlayerId().equals(playerId) && this.getStack().isEmpty() && this.isMainPhase())   message+=" - play spells and sorceries.";
 else   message+=" - play instants and activated abilites.";
  playerQueryEventSource.select(playerId,message);
}
