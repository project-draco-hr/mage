{
  if (!simulation) {
    saveState();
    if (logger.isDebugEnabled())     logger.debug("Bookmarking state: " + gameStates.getSize());
    savedStates.push(gameStates.getSize() - 1);
    return savedStates.size();
  }
  return 0;
}
