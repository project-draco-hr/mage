{
  if (playerId == null) {
    boolean result=checkIfGameIsOver();
    return result;
  }
 else {
    logger.debug("game.gameOver -> player leaves " + playerId);
    leave(playerId);
    return true;
  }
}
