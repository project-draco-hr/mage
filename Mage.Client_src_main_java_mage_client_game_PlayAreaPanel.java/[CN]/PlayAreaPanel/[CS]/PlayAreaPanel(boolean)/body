{
  initComponents();
  setOpaque(false);
  battlefieldPanel.setOpaque(false);
  popupMenu=new JPopupMenu();
  if (isPlayer) {
    addPopupMenuPlayer();
  }
 else {
    addPopupMenuWatcher();
  }
  this.add(popupMenu);
}
