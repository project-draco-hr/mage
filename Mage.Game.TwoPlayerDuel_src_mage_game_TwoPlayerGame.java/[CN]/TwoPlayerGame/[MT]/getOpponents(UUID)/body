{
  List<UUID> opponents=new ArrayList<UUID>();
  for (  Player player : this.getPlayers().values()) {
    if (!player.getId().equals(playerId))     opponents.add(player.getId());
  }
  return opponents;
}
