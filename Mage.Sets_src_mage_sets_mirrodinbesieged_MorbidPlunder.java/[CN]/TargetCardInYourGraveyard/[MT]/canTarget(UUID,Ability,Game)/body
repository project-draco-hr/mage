{
  Card card=game.getCard(id);
  if (card != null && game.getZone(card.getId()) == Zone.GRAVEYARD && card.getOwnerId().equals(source.getControllerId()))   return filter.match(card);
  return false;
}
