{
  if (!super.match(permanent))   return notFilter;
  if (useAttacking && permanent.isAttacking() != attacking)   return notFilter;
  if (useBlocking && permanent.isBlocking() != blocking)   return notFilter;
  return !notFilter;
}
