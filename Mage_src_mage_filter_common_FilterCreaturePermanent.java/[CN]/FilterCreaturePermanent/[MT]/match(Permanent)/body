{
  if (!super.match(permanent))   return false;
  if (useAttacking && permanent.isAttacking() != attacking)   return false;
  if (useBlocking && permanent.isBlocking() != blocking)   return false;
  return true;
}
