{
  combat.hideDialog();
  pickNumber.hide();
  for (  ExileZoneDialog exile : exiles.values()) {
    exile.hide();
  }
  for (  ShowCardsDialog reveal : revealed.values()) {
    reveal.hide();
  }
}
