{
  Player p=game.getPlayer(source.getControllerId());
  if (p != null) {
    for (    UUID oppId : p.getInRange()) {
      Player opponent=game.getPlayer(oppId);
      if (opponent != null && opponent.getGraveyard().size() >= value)       return true;
    }
  }
  return false;
}
