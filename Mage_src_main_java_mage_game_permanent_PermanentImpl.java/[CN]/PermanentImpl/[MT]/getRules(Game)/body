{
  try {
    List<String> rules=getRules();
    if (info != null) {
      for (      String data : info.values()) {
        rules.add(data);
      }
    }
    return rules;
  }
 catch (  Exception e) {
    return rulesError;
  }
}
