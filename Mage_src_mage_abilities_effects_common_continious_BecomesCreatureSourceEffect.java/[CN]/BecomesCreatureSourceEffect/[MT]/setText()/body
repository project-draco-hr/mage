{
  if (type.length() > 0)   staticText=duration.toString() + " {this} becomes a " + token.getDescription()+ " that's still a "+ this.type;
 else   staticText=duration.toString() + " {this} becomes a " + token.getDescription();
}
