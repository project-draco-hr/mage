{
  if (event.getType() == EventType.TAPPED_FOR_MANA && game.getCard(event.getSourceId()).getCardType().contains(CardType.LAND) && event.getPlayerId().equals(this.controllerId)) {
    return true;
  }
  return false;
}
