{
  Permanent archmage=game.getPermanent(source.getSourceId());
  Player you=game.getPlayer(source.getControllerId());
  if (event.getPlayerId().equals(source.getControllerId()) && archmage != null && you != null && you.chooseUse(Outcome.Benefit,"Would you like to skip drawing a card?",source,game)) {
    return true;
  }
  return false;
}
