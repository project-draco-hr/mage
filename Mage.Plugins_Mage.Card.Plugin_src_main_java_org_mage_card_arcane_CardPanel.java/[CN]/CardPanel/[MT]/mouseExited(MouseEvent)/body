{
  if (getMousePosition(true) != null)   return;
  if (popupShowing) {
synchronized (this) {
      if (popupShowing) {
        popupShowing=false;
        callback.mouseExited(e);
      }
    }
  }
}
