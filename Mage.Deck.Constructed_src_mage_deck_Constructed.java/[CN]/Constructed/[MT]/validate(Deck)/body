{
  if (deck.getCards().size() < 60)   return false;
  if (deck.getSideboard().size() != 0 && deck.getSideboard().size() != 15)   return false;
  List<String> basicLandNames=new ArrayList<String>(Arrays.asList("Forest","Island","Mountain","Swamp","Plains"));
  Map<String,Integer> counts=new HashMap<String,Integer>();
  countCards(counts,deck.getCards());
  countCards(counts,deck.getSideboard());
  for (  Entry<String,Integer> entry : counts.entrySet()) {
    if (entry.getValue() > 4) {
      if (!basicLandNames.contains(entry.getKey())) {
        return false;
      }
    }
  }
  return true;
}
