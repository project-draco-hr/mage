{
  Permanent permanent=game.getPermanent(id);
  if (permanent != null) {
    if (source != null)     return permanent.canBeTargetedBy(game.getObject(source.getSourceId())) && filter.match(permanent,controllerId,game);
 else     return filter.match(permanent,controllerId,game);
  }
  return false;
}
