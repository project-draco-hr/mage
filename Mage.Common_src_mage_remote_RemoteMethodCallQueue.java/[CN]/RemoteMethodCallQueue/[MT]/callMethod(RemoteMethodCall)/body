{
synchronized (call) {
    try {
      this.offer(call);
      call.wait();
      if (call.isException()) {
        if (call.getException() != null)         throw call.getException();
 else         throw new ServerUnavailable();
      }
    }
 catch (    InterruptedException ex) {
    }
  }
}
