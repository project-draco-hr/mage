{
  Card source=game.getPermanentOrLKIBattlefield(event.getSourceId());
  if (event.getPlayerId().equals(getControllerId()) && source != null && filter.match(source,game)) {
    return true;
  }
  return false;
}
