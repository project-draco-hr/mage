{
  if (hand.size() > this.maxHandSize) {
    if (!game.isSimulation())     game.informPlayers(getLogName() + " discards down to " + this.maxHandSize+ (this.maxHandSize == 1 ? " hand card" : " hand cards"));
    discard(hand.size() - this.maxHandSize,null,game);
  }
}
