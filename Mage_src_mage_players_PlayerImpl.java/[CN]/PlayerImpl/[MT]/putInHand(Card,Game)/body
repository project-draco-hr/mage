{
  if (card.getOwnerId().equals(playerId)) {
    this.hand.add(card);
    if (card.getSpellAbility() != null)     card.getSpellAbility().clear();
  }
 else {
    return game.getPlayer(card.getOwnerId()).putInHand(card,game);
  }
  return true;
}
