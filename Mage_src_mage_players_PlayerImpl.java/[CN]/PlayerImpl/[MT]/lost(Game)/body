{
  if (canLose(game)) {
    this.loses=true;
    if (!this.wins) {
      game.fireEvent(GameEvent.getEvent(GameEvent.EventType.LOST,null,null,playerId));
    }
    if (!hasLeft()) {
      game.leave(playerId);
    }
  }
}
