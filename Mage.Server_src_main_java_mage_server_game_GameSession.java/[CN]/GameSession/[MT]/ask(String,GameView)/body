{
  if (!killed) {
    setupTimeout();
    Session session=SessionManager.getInstance().getSession(sessionId);
    if (session != null)     session.fireCallback(new ClientCallback("gameAsk",game.getId(),new GameClientMessage(gameView,question)));
  }
}
