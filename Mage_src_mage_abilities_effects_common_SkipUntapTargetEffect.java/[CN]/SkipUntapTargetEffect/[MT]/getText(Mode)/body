{
  if (staticText != null) {
    return staticText;
  }
  return new StringBuilder("Target ").append(mode.getTargets().get(0).getTargetName()).append(" doesn't untap ").append(getDuration().toString()).toString();
}
