{
  return new StringBuilder("Vanishing ").append(vanishingAmount).append(" <i>(This permanent enters the battlefield with ").append(CardUtil.numberToText(vanishingAmount)).append(" time counters on it. At the beginning of your upkeep, remove a time counter from it. When the last is removed, sacrifice it.)<i>").toString();
}
