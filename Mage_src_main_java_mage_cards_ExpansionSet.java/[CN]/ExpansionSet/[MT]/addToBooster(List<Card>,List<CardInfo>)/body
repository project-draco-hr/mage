{
  if (!cards.isEmpty()) {
    CardInfo cardInfo=cards.remove(rnd.nextInt(cards.size()));
    if (cardInfo != null) {
      Card card=cardInfo.getCard();
      if (card != null) {
        booster.add(card);
      }
    }
  }
}
