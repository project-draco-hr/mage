{
  if (turn.currentPhase != null)   this.currentPhase=turn.currentPhase.copy();
  this.activePlayerId=turn.activePlayerId;
  for (  Phase phase : turn.phases) {
    this.phases.add(phase.copy());
  }
}
