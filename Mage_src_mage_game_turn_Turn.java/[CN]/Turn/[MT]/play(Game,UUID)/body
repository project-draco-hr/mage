{
  if (game.isGameOver())   return;
  phase=null;
  step=null;
  this.activePlayerId=activePlayerId;
  game.getPlayer(activePlayerId).beginTurn();
  while (nextStep(game)) {
    playStep(game);
    game.saveState();
  }
}
