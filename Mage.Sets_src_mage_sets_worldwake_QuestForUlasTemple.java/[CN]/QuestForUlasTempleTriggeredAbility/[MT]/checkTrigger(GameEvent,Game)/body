{
  Permanent quest=game.getPermanent(super.getSourceId());
  if (event.getType() == GameEvent.EventType.END_TURN_STEP_PRE && quest != null && quest.getCounters().getCount(CounterType.QUEST) >= 3) {
    return true;
  }
  return false;
}
