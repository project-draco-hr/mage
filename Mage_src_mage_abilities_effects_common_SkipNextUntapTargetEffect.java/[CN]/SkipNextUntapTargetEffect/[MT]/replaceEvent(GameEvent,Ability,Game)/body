{
  if (targetPointer.getTargets(game,source).size() < 2) {
    used=true;
  }
 else {
    count++;
  }
  if (count == targetPointer.getTargets(game,source).size()) {
    used=true;
  }
  return true;
}
