{
  if (source.getTargets().get(0).getTargets().size() < 2) {
    used=true;
  }
 else {
    count++;
  }
  if (count == source.getTargets().get(0).getTargets().size()) {
    used=true;
  }
  return true;
}
