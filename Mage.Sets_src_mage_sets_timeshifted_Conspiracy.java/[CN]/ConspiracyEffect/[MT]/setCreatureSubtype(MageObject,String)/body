{
  if (object != null && (object.getSubtype().size() != 1 || !object.getSubtype().contains(subtype))) {
    object.getSubtype().removeAll(CardRepository.instance.getCreatureTypes());
    object.getSubtype().add(subtype);
  }
}
