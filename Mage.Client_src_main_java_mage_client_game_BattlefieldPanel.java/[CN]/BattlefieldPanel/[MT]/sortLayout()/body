{
  if (battlefield == null || this.getWidth() < 1) {
    return;
  }
  layoutStrategy.doLayout(this,width);
  this.jScrollPane.repaint();
  this.jScrollPane.revalidate();
  invalidate();
  repaint();
}
