{
  try {
    GamesRoom room=GamesRoomManager.getInstance().getRoom(roomId);
    if (room != null) {
      return room.getPlayers();
    }
 else {
      return null;
    }
  }
 catch (  Exception ex) {
    handleException(ex);
  }
  return null;
}
