{
  activateCount=0;
  while (true) {
    this.costs.clearPaid();
    this.manaCostsToPay.clearPaid();
    if (!super.activate(game,noMana))     break;
    activateCount++;
  }
  kicked=activateCount > 0;
  return kicked;
}
