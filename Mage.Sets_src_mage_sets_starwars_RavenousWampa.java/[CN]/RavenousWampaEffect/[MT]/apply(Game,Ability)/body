{
  Permanent p=null;
  if (p != null) {
    Player player=game.getPlayer(source.getControllerId());
    if (player != null) {
      player.gainLife(p.getToughness().getValue(),game);
      return true;
    }
  }
  return false;
}
