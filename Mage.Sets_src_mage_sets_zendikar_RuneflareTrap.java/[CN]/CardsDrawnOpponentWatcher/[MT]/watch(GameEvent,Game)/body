{
  if (event.getType() == GameEvent.EventType.DREW_CARD && game.getOpponents(controllerId).contains(event.getPlayerId())) {
    cardsDrawn+=1;
    if (cardsDrawn >= 3) {
      condition=true;
    }
  }
}
