{
  if (event.getType() == GameEvent.EventType.ENTERS_THE_BATTLEFIELD && event.getTargetId().equals(this.getSourceId()) && KickedCondition.getInstance().apply(game,this)) {
    return true;
  }
  return false;
}
