{
  if (event.getType() == GameEvent.EventType.ZONE_CHANGE && ((ZoneChangeEvent)event).isDiesEvent() && ((ZoneChangeEvent)event).getPlayerId().equals(this.getControllerId())) {
    cards.add(event.getTargetId());
  }
}
