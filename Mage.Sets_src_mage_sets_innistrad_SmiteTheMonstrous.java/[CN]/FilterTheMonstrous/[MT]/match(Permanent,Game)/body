{
  if (!super.match(permanent,game))   return notFilter;
  if (!permanent.getCardType().contains(CardType.CREATURE))   return notFilter;
  if (permanent.getPower().getValue() < 4)   return notFilter;
  return !notFilter;
}
