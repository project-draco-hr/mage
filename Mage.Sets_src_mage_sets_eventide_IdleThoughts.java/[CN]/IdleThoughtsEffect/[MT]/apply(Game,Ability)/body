{
  Condition condition=new CardsInHandCondition();
  Player you=game.getPlayer(source.getControllerId());
  if (condition.apply(game,source) && you != null) {
    you.drawCards(1,game);
    return true;
  }
  return false;
}
