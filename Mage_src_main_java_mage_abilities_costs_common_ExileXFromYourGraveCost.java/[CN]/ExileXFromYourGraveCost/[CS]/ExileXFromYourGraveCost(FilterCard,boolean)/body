{
  super(new StringBuilder(filter.getMessage()).append(" to exile").toString());
  this.filter=filter;
  this.text=new StringBuilder(additionalCostText ? "As an additional cost to cast {source}, exile " : "Exile ").append(xText).append(" ").append(filter.getMessage()).toString();
}
