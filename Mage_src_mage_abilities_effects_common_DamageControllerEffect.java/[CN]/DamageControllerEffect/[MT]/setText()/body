{
  StringBuilder sb=new StringBuilder();
  sb.append("{source} deals ").append(Integer.toString(amount)).append(" damage to you");
  if (!preventable)   sb.append(". The damage can't be prevented");
  staticText=sb.toString();
}
