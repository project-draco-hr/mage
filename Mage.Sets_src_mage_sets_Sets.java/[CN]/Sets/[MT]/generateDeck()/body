{
  List<ColoredManaSymbol> allowedColors=new ArrayList<ColoredManaSymbol>();
  int numColors=rnd.nextInt(2) + 1;
  int cardPoolSize=60;
  if (numColors > 2) {
    cardPoolSize+=20;
  }
  Deck deck=new Deck();
  return deck;
}
