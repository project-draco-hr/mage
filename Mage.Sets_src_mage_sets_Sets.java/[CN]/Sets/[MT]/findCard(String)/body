{
  for (  ExpansionSet set : fINSTANCE.values()) {
    for (    Card card : set.createCards()) {
      if (name.equals(card.getName()))       return card.getClass().getCanonicalName();
    }
  }
  return null;
}
