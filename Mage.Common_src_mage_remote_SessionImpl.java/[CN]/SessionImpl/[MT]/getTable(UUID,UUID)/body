{
  try {
    if (isConnected())     return server.getTable(roomId,tableId);
  }
 catch (  MageException ex) {
    handleMageException(ex);
  }
catch (  Throwable t) {
    handleThrowable(t);
  }
  return null;
}
