{
  try {
    if (isConnected()) {
      server.setManaPoolMode(gameId,sessionId,autoPayment);
      return true;
    }
  }
 catch (  MageException ex) {
    handleMageException(ex);
  }
catch (  Throwable t) {
    handleThrowable(t);
  }
  return false;
}
