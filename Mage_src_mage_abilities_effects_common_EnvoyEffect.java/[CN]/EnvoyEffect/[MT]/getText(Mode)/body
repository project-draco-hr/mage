{
  if (staticText != null && !staticText.isEmpty()) {
    return staticText;
  }
  return "Reveal the top " + CardUtil.numberToText(numCards) + " cards of your library. Put all "+ filter.getMessage()+ " revealed this way into your hand and the rest on the bottom of your library in any order.";
}
