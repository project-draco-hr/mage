{
  this.client=replayClient;
  game.start();
  rmiExecutor.submit(new Runnable(){
    public void run(){
      try {
        client.init(new GameView(game.next()));
      }
 catch (      RemoteException ex) {
        logger.log(Level.SEVERE,null,ex);
      }
    }
  }
);
}
