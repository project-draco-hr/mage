{
  updateGameStatePriority("chooseMulligan",game);
  int nextHandSize=game.mulliganDownTo(playerId);
  game.fireAskPlayerEvent(playerId,new StringBuilder("Mulligan ").append(getHand().size() > nextHandSize ? "down" : "").append(" to ").append(nextHandSize).append(nextHandSize == 1 ? " card?" : " cards?").toString());
  waitForBooleanResponse();
  if (!abort) {
    return response.getBoolean();
  }
  return false;
}
