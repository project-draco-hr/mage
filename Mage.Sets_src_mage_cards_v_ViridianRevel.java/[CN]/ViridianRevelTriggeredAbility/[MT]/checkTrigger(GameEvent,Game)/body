{
  if (((ZoneChangeEvent)event).isDiesEvent()) {
    Card card=game.getCard(event.getTargetId());
    Player controller=game.getPlayer(getControllerId());
    if (controller != null && card != null && card.getCardType().contains(CardType.ARTIFACT) && controller.hasOpponent(card.getOwnerId(),game)) {
      return true;
    }
  }
  return false;
}
