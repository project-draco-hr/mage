{
switch (event.getType()) {
case DAMAGE_PLAYER:
    event.setAmount(event.getAmount() * 2);
  return true;
case DAMAGE_CREATURE:
event.setAmount(event.getAmount() * 2);
return true;
}
return false;
}
