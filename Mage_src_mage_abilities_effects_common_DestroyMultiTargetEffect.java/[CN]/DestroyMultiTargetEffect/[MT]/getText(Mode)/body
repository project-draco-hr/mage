{
  StringBuilder sb=new StringBuilder();
  sb.append("Destroy ");
  for (  Target target : mode.getTargets()) {
    if (target.getNumberOfTargets() > 1)     sb.append(target.getNumberOfTargets());
    sb.append("target ").append(target.getTargetName()).append(" and ");
  }
  return sb.toString();
}
