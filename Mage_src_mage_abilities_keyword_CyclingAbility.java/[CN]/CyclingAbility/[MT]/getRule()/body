{
  String rule;
  if (cost instanceof ManaCosts) {
    rule=this.text + " " + cost.getText()+ " <i>("+ super.getRule()+ ")</i>";
  }
 else {
    rule=this.text + "-" + cost.getText()+ " <i>("+ super.getRule()+ ")</i>";
  }
  return rule;
}
