{
  String rule="";
  if (cost instanceof ManaCosts) {
    rule="Cycling " + cost.getText() + " <i>("+ super.getRule()+ ")</i>";
  }
 else {
    rule="Cycling-" + cost.getText() + " <i>("+ super.getRule()+ ")</i>";
  }
  return rule;
}
