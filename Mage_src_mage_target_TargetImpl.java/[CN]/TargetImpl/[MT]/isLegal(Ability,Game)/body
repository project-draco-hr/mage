{
  for (  UUID targetId : targets.keySet()) {
    if (!game.replaceEvent(GameEvent.getEvent(EventType.TARGET,targetId,source.getId(),source.getControllerId())))     return true;
    if (canTarget(targetId,source,game))     return true;
  }
  return false;
}
