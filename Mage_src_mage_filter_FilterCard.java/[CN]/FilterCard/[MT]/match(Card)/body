{
  if (!super.match(card))   return false;
  if (ownerId.size() > 0 && ownerId.contains(card.getOwnerId()) == notOwner)   return false;
  if (expansionSetId.size() > 0 && expansionSetId.contains(card.getExpansionSetId()) == notExpansionSetId)   return false;
  return true;
}
