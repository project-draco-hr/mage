{
  if (!killed) {
    setupTimeout(20);
    Session session=SessionManager.getInstance().getSession(sessionId);
    if (session != null)     session.fireCallback(new ClientCallback("draftPick",new DraftClientMessage(draftPickView)));
  }
}
