{
  if (staticText != null && !staticText.isEmpty()) {
    return staticText;
  }
  return "{this} becomes " + (setColor == null ? "the color of your choice" : setColor.getDescription()) + " "+ duration.toString();
}
