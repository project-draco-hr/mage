{
  if (type.length() > 0)   staticText="Until end of turn {this} becomes a " + token.getDescription() + " that's still a "+ this.type;
 else   staticText="Until end of turn {this} becomes a " + token.getDescription();
}
