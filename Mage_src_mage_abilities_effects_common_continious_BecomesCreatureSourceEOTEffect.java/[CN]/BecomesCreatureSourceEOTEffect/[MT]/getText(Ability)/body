{
  if (type.length() > 0)   return "Until end of turn {this} becomes a " + token.getDescription() + " that's still a "+ this.type;
 else   return "Until end of turn {this} becomes a " + token.getDescription();
}
