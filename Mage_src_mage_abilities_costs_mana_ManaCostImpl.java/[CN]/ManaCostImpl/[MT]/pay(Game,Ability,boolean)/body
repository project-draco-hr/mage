{
  if (noMana) {
    setPaid();
    return true;
  }
  Player player=game.getPlayer(source.getControllerId());
  assignPayment(player.getManaPool());
  while (!isPaid()) {
    if (player.playMana(this,game))     assignPayment(player.getManaPool());
 else     return false;
  }
  return true;
}
