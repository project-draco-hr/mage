{
  if (this.cardId == null || !this.cardId.equals(cardId)) {
    if (this.panel != null) {
      remove(this.panel);
    }
    this.cardId=cardId;
    bigImage=image;
synchronized (this) {
      source=null;
      hue=0.000f;
    }
    drawText(strings);
    setFoil(foil);
  }
}
