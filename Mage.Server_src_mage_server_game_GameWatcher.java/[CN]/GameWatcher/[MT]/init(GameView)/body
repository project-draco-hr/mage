{
  if (!killed)   rmiExecutor.submit(new Runnable(){
    @Override public void run(){
      try {
        client.init(gameView);
      }
 catch (      RemoteException ex) {
        handleRemoteException(ex);
      }
    }
  }
);
}
