{
  if (cards == null) {
synchronized (this) {
      if (cards == null) {
        this.cards=getCardClassesForPackage(packageName);
        this.rarities=getCardsByRarity();
      }
    }
  }
  return cards;
}
