{
  if (!getRarities().containsKey(rarity))   return null;
  int size=getRarities().get(rarity).size();
  if (size > 0) {
    return getRarities().get(rarity).get(rnd.nextInt(size)).copy();
  }
  return null;
}
