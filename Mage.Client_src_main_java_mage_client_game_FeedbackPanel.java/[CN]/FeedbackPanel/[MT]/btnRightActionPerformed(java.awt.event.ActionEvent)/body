{
  this.selected=true;
  clear();
  if (mode == FeedbackMode.SELECT && (evt.getModifiers() & ActionEvent.CTRL_MASK) == ActionEvent.CTRL_MASK)   session.sendPlayerInteger(gameId,0);
 else   session.sendPlayerBoolean(gameId,false);
}
