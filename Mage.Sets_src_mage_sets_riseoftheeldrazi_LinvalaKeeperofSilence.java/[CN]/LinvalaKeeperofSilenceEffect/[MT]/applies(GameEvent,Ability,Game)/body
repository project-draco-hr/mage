{
  Permanent p=game.getPermanent(event.getTargetId());
  if (event.getType() == GameEvent.EventType.ACTIVATE_ABILITY && game.getOpponents(source.getControllerId()).contains(event.getPlayerId()) && p != null && p.getCardType().contains(CardType.CREATURE)) {
    return true;
  }
  return false;
}
