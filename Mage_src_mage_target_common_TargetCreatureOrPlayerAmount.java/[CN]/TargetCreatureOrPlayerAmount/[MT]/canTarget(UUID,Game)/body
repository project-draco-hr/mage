{
  Permanent permanent=game.getPermanent(id);
  MageObject targetSource=game.getObject(this.source.getSourceId());
  if (permanent != null) {
    if (this.source != null)     return permanent.canBeTargetedBy(targetSource) && filter.match(permanent);
 else     return filter.match(permanent);
  }
  Player player=game.getPlayer(id);
  if (player != null)   if (this.source != null)   return player.canBeTargetedBy(targetSource) && filter.match(player);
 else   return filter.match(player);
  return false;
}
