{
  if (condition == true)   return;
  if (event.getType() == EventType.DAMAGED_CREATURE) {
    Card card=game.getCard(sourceId);
    if (card != null && card.getSpellAbility().getId().equals(event.getSourceId()))     condition=true;
  }
}
