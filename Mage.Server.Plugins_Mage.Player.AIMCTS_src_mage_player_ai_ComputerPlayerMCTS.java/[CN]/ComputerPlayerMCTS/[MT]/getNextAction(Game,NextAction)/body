{
  if (root != null) {
    MCTSNode newRoot=null;
    newRoot=root.getMatchingState(game.getState().getValue(false,game));
    if (newRoot != null)     newRoot.emancipate();
 else     logger.info("unable to find matching state");
    root=newRoot;
  }
  calculateActions(game,nextAction);
}
