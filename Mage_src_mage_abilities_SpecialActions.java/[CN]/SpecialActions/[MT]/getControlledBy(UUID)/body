{
  LinkedHashMap<UUID,SpecialAction> controlledBy=new LinkedHashMap<UUID,SpecialAction>();
  for (  SpecialAction action : this) {
    if (action.getControllerId().equals(controllerId))     controlledBy.put(action.id,action);
  }
  return controlledBy;
}
