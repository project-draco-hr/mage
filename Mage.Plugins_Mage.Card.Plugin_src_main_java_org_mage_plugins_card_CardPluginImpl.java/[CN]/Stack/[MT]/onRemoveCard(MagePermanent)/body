{
  if (card != null) {
    Animation.hideCard((CardPanel)card);
    try {
      while ((card).getAlpha() - 0.05f > 0) {
        Thread.sleep(30);
      }
    }
 catch (    Exception e) {
      e.printStackTrace();
    }
  }
}
