{
  return new StringBuilder("Level up ").append(manaCostsToPay.getText()).append(" <i>(").append(manaCostsToPay.getText()).append(": Put a level counter on this. Level up only as a sorcery.)</i>").toString();
}
