{
  while (boosterNum < sets.size()) {
    openBooster();
    while (pickCards()) {
      if (boosterNum % 2 == 1)       passLeft();
 else       passRight();
      fireUpdatePlayersEvent();
    }
  }
  startTournament();
}
