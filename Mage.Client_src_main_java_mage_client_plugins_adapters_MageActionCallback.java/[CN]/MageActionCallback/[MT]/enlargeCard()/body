{
  if (!enlarged) {
    enlarged=true;
    CardView card=null;
    if (popupData != null) {
      card=popupData.card;
    }
    if (this.state) {
      hidePopup();
    }
    if (card != null) {
      displayCard(card,popupData);
    }
  }
}
