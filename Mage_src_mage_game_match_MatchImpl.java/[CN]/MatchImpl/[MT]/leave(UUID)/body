{
  MatchPlayer mPlayer=getPlayer(playerId);
  if (mPlayer != null) {
    boolean result=players.remove(mPlayer);
synchronized (this) {
      this.notifyAll();
    }
    return result;
  }
  return false;
}
