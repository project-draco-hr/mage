{
  if (event.getType() == GameEvent.EventType.UPKEEP_STEP_PRE && event.getPlayerId().equals(this.controllerId) && game.getBattlefield().count(filter,this.controllerId,game) > 6) {
    return true;
  }
  return false;
}
