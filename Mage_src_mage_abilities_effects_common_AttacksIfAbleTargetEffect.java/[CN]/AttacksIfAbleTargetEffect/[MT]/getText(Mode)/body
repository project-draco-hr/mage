{
  if (this.duration == Duration.EndOfTurn)   return "Target " + mode.getTargets().get(0).getTargetName() + " attacks this turn if able";
 else   return "Target " + mode.getTargets().get(0).getTargetName() + " attacks each turn if able";
}
