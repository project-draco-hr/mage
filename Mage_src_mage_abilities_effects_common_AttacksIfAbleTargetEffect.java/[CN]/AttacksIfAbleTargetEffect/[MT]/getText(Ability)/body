{
  if (this.duration == Duration.EndOfTurn)   return "Target " + source.getTargets().get(0).getTargetName() + " attacks this turn if able";
 else   return "Target " + source.getTargets().get(0).getTargetName() + " attacks each turn if able";
}
