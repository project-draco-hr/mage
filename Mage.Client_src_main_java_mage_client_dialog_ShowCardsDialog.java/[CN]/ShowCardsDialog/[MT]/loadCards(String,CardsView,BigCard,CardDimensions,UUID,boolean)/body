{
  this.title=name;
  cardArea.removeAll();
  if (showCards != null && showCards.size() < 10)   loadCardsFew(showCards,bigCard,dimension,gameId);
 else   loadCardsMany(showCards,bigCard,dimension,gameId);
  cardArea.revalidate();
  if (getParent() != MageFrame.getDesktop()) {
    MageFrame.getDesktop().add(this,JLayeredPane.POPUP_LAYER);
  }
  pack();
  this.revalidate();
  this.repaint();
  this.setModal(modal);
  this.setVisible(true);
}
