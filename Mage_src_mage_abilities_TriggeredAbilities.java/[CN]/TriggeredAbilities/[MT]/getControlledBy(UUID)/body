{
  TriggeredAbilities controlledBy=new TriggeredAbilities();
  for (  TriggeredAbility ability : this) {
    if (ability.getControllerId().equals(controllerId))     controlledBy.add(ability);
  }
  return controlledBy;
}
