{
  while (true) {
    PhaseStep extraStep=game.getState().getTurnMods().extraStep(activePlayerId,afterStep);
    if (extraStep == null)     return;
    Step step=new Step(extraStep);
    currentStep=step;
    step.play(game,activePlayerId);
  }
}
