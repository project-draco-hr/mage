{
  ManaCosts<T> unpaid=new ManaCostsImpl<T>();
  for (  ManaCost cost : this) {
    if (cost instanceof VariableManaCost && !cost.isPaid())     unpaid.add((T)cost.getUnpaid());
  }
  return unpaid;
}
