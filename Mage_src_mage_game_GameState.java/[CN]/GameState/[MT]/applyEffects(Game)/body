{
  for (  Player player : players.values()) {
    player.reset();
  }
  battlefield.reset(game);
  combat.reset();
  resetOtherAbilities();
  effects.apply(game);
  battlefield.fireControlChangeEvents(game);
}
