{
  if (this.size() > 0 && canChoose(playerId,game)) {
    while (!isChosen()) {
      Target target=this.getUnchosen().get(0);
      if (!target.choose(outcome,playerId,game))       return false;
    }
  }
  return true;
}
