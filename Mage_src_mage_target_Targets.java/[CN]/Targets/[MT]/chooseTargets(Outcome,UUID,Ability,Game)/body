{
  if (this.size() > 0) {
    if (!canChoose(source.getId(),playerId,game))     return false;
    while (!isChosen()) {
      Target target=this.getUnchosen().get(0);
      if (!target.chooseTarget(outcome,playerId,source,game))       return false;
    }
  }
  return true;
}
