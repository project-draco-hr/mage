{
  if (this.size() > 0) {
    Player player=game.getPlayer(this.source.getControllerId());
    while (!isChosen()) {
      Target target=this.getUnchosen().get(0);
      if (!player.chooseTarget(outcome,target,game))       return false;
    }
  }
  return true;
}
