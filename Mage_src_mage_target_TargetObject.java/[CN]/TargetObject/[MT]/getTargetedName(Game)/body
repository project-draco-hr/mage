{
  StringBuilder sb=new StringBuilder();
  for (  UUID targetId : getTargets()) {
    MageObject object=game.getObject(targetId);
    if (object != null)     sb.append(object.getName()).append(" ");
  }
  return sb.toString();
}
