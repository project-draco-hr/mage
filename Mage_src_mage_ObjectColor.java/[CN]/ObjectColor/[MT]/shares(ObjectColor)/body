{
  if (this == color)   return true;
  if (!hasColor() && !color.hasColor())   return true;
  return color.white && white || color.blue && blue || color.black && black || color.red && red || color.green && green;
}
