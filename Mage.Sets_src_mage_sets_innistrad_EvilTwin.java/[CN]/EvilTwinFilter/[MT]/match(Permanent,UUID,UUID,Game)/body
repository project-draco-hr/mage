{
  if (!super.match(permanent))   return notFilter;
  Permanent twin=game.getPermanent(sourceId);
  if (twin != null) {
    if (!permanent.getName().equals(twin.getName()))     return false;
  }
  return !notFilter;
}
