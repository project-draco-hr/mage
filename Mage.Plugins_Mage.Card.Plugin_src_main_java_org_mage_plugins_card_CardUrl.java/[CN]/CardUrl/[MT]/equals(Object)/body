{
  if (other == null) {
    return false;
  }
  if (other instanceof CardUrl) {
    return name.equals(((CardUrl)other).name) && set.equals(((CardUrl)other).set) && collector.equals(((CardUrl)other).collector)&& token == ((CardUrl)other).token;
  }
  return false;
}
