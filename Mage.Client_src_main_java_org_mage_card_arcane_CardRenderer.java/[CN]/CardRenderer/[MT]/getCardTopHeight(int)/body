{
  float BOX_HEIGHT_FRAC=0.065f;
  int BOX_HEIGHT_MIN=16;
  int boxHeight=(int)Math.max(BOX_HEIGHT_MIN,BOX_HEIGHT_FRAC * cardWidth * 1.4f);
  int borderWidth=getBorderWidth(cardWidth);
  return 2 * borderWidth + boxHeight;
}
