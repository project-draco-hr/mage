{
  if (event.getType() == GameEvent.EventType.CLEANUP_STEP_POST) {
    changedControllerOR1stTurn=false;
  }
  if (event.getType() == GameEvent.EventType.LOST_CONTROL && event.getSourceId() == cardId) {
    changedControllerOR1stTurn=true;
  }
}
