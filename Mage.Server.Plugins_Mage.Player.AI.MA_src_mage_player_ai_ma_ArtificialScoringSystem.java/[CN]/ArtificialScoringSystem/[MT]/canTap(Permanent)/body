{
  return !permanent.isTapped() && (!permanent.hasSummoningSickness() || !permanent.getCardType().contains(Constants.CardType.CREATURE) || permanent.getAbilities().contains(HasteAbility.getInstance()));
}
