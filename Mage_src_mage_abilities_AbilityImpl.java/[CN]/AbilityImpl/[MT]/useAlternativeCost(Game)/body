{
  for (  AlternativeCost cost : alternativeCosts) {
    if (cost.isAvailable(game)) {
      if (game.getPlayer(this.controllerId).chooseUse(Outcome.Neutral,"Use alternative cost " + cost.getName(),game))       return cost.pay(game,false);
    }
  }
  return false;
}
