{
  Player player=game.getPlayer(source.getFirstTarget());
  if (player != null) {
    for (int i=0; i < amount; i++) {
      Card card=player.getLibrary().removeFromTop(game);
      if (card != null)       player.getGraveyard().add(card);
 else       break;
    }
  }
  return true;
}
