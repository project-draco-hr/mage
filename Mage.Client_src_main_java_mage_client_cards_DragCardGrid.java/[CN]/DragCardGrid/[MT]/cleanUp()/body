{
  for (  MageCard cardView : cardViews.values()) {
    cardContent.remove(cardView);
  }
  cardGrid.clear();
  maxStackSize.clear();
  allCards.clear();
  lastBigCard=null;
  clearCardEventListeners();
}
