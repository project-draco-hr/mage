{
  for (int i=0; i < string.size(); i++) {
    String stringToCheck=string.get(i);
    if (stringToCheck == null)     continue;
    for (int j=0; j < string.size(); j++) {
      if (i == j)       continue;
      String stringToCompare=string.get(j);
      if (stringToCheck.equals(stringToCompare)) {
        return true;
      }
    }
  }
  return false;
}
