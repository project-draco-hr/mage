{
  boolean result=false;
  if (blocker.getCardType().contains(Constants.CardType.ARTIFACT) && (blocker.getCardType().contains(Constants.CardType.CREATURE)))   result=true;
  if (attacker.getColor().shares(blocker.getColor()))   result=true;
  return result;
}
