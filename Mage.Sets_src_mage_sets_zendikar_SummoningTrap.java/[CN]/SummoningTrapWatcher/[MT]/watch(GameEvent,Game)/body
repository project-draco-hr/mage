{
  if (condition == true)   return;
  if (event.getType() == EventType.COUNTER && game.getCard(event.getTargetId()).getCardType().contains(CardType.CREATURE) && game.getOpponents(controllerId).contains(event.getPlayerId()))   condition=true;
}
