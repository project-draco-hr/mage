{
  if (source.getTargets().get(0).getNumberOfTargets() == 1)   return "The controller of target " + source.getTargets().get(0).getTargetName() + " sacrifices it";
 else   return "The controller of " + source.getTargets().get(0).getNumberOfTargets() + " target "+ source.getTargets().get(0).getTargetName()+ " sacrifices it";
}
