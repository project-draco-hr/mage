{
  if (this.controllerId != null && this.controllerId.equals(playerId))   return true;
 else {
    Card card=(Card)game.getObject(this.sourceId);
    if (card != null)     return card.getOwnerId().equals(playerId);
  }
  return false;
}
