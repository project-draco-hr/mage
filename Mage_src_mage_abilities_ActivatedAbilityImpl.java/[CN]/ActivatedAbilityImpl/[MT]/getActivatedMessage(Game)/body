{
  if (game.isSimulation()) {
    return "";
  }
  MageObject object=game.getObject(this.sourceId);
  return new StringBuilder(" activates ").append(object != null ? this.getRule(object.getName()) : this.getRule()).append(" from ").append(getMessageText(game)).toString();
}
