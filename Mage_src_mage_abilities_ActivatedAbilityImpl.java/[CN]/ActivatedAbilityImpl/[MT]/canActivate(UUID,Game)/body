{
  if (!controlsAbility(playerId,game))   return false;
  if (timing == TimingRule.INSTANT || (game.isMainPhase() && game.getStack().isEmpty() && game.getActivePlayerId().equals(playerId)) && costs.canPay(playerId,game) && targets.canChoose(sourceId,game)) {
    return true;
  }
  return false;
}
