{
  if (!controlsAbility(playerId,game))   return false;
  if ((timing == TimingRule.INSTANT || game.canPlaySorcery(playerId)) && costs.canPay(this,game) && targets.canChoose(sourceId,playerId,game)) {
    return true;
  }
  return false;
}
