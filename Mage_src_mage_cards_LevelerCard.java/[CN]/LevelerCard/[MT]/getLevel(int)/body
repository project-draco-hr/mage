{
  for (  LevelAbility levelerLevel : levels) {
    if (level >= levelerLevel.getLevel1() && (levelerLevel.getLevel2() == -1 || level <= levelerLevel.getLevel2()))     return levelerLevel;
  }
  return null;
}
