{
  new Animation(600){
    protected void start(){
    }
    protected void update(    float percentage){
      float alpha=1 - percentage;
      card.setAlpha(alpha);
      card.repaint();
    }
    protected void end(){
    }
  }
;
}
