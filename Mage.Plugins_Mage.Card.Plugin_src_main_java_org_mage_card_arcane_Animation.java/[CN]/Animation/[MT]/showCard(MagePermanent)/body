{
  new Animation(600){
    protected void start(){
    }
    protected void update(    float percentage){
      float alpha=percentage;
      card.setAlpha(alpha);
      card.repaint();
    }
    protected void end(){
      card.setAlpha(1.f);
    }
  }
;
}
