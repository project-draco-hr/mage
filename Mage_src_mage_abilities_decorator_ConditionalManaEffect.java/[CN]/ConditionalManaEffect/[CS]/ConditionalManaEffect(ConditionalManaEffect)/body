{
  super(effect);
  this.effect=(ManaEffect)effect.effect.copy();
  if (effect.otherwiseEffect != null)   this.otherwiseEffect=(ManaEffect)effect.otherwiseEffect.copy();
  this.condition=effect.condition;
}
