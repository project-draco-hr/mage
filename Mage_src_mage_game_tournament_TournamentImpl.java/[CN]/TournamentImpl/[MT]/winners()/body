{
  for (  TournamentPlayer winner : this.getActivePlayers()) {
    winner.setState(TournamentPlayerState.FINISHED);
    if (options.getNumberRounds() == 0) {
      winner.setStateInfo("Winner");
    }
  }
}
