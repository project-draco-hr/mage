{
  List<VariableManaCost> variableCosts=new ArrayList<VariableManaCost>();
  for (  T cost : this) {
    if (cost instanceof ManaCosts)     variableCosts.addAll(((ManaCosts)cost).getVariableCosts());
  }
  return variableCosts;
}
