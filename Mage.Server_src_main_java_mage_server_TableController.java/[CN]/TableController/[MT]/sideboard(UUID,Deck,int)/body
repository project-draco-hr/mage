{
  for (  Entry<UUID,UUID> entry : userPlayerMap.entrySet()) {
    if (entry.getValue().equals(playerId)) {
      User user=UserManager.getInstance().getUser(entry.getKey());
      if (user != null)       user.sideboard(deck,table.getId(),timeout);
      break;
    }
  }
}
