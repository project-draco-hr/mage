{
  Player player=game.getPlayer(id);
  if (player != null) {
    if (source != null)     return player.canTarget(game.getObject(this.source.getSourceId())) && filter.match(player);
 else     return filter.match(player);
  }
  return false;
}
