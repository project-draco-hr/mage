{
  Session session=sessions.get(sessionId);
  sessions.remove(sessionId);
  if (session != null) {
    if (voluntary)     session.kill();
 else     session.disconnect();
  }
 else {
    logger.info("could not find session with id " + sessionId);
  }
}
