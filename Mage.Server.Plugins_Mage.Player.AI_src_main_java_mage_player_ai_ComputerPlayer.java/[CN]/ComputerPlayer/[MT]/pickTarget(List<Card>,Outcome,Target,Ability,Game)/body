{
  Card card=null;
  while (!cards.isEmpty()) {
    if (outcome.isGood()) {
      card=pickBestCard(cards,null);
    }
 else {
      card=pickWorstCard(cards,null);
    }
    if (target.canTarget(card.getId(),source,game)) {
      return card;
    }
    cards.remove(card);
  }
  return null;
}
