{
  Card card=game.getCard(id);
  if (card != null && game.getZone(card.getId()) == Zone.GRAVEYARD)   if (game.getPlayer(source.getControllerId()).getGraveyard().contains(id))   return filter.match(card);
  return false;
}
