{
  StringBuilder sb=new StringBuilder();
  if (amount == 1)   sb.append("put a");
 else   sb.append("put ").append(Integer.toString(amount));
  sb.append(" ").append(token.getDescription()).append(" onto the battlefield");
  return sb.toString();
}
