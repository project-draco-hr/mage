{
  MageObject o=game.getObject(source.getTargets().getFirstTarget());
  if (o != null) {
    if (o instanceof Permanent || o instanceof StackObject)     o.getColor().setColor(setColor);
  }
  return false;
}
