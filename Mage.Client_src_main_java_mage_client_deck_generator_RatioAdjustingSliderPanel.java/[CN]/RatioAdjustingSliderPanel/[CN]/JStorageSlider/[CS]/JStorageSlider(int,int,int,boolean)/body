{
  super(min,max,value);
  defaultValue=value;
  this.adjust=adjust;
  setMinorTickSpacing(5);
  setMajorTickSpacing(10);
  setPaintTicks(true);
  setPaintLabels(true);
  setLabelTable(createStandardLabels(10));
}
