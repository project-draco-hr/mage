{
  try {
    setEditable(true);
    kit.insertHTML(doc,doc.getLength(),s,0,0,null);
    int len=getDocument().getLength();
    setCaretPosition(len);
    setEditable(false);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
