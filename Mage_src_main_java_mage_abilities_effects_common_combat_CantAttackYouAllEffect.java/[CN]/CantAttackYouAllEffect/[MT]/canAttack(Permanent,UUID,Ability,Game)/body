{
  return (!alsoPlaneswalker && !defenderId.equals(source.getControllerId())) || (alsoPlaneswalker && !game.getCombat().getDefendingPlayerId(attacker.getId(),game).equals(source.getControllerId()));
}
