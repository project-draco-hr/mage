{
  if (event.getAmount() >= this.amount) {
    event.setAmount(event.getAmount() - amount);
    this.used=true;
  }
 else {
    int damage=event.getAmount();
    event.setAmount(0);
    amount-=damage;
  }
  return false;
}
