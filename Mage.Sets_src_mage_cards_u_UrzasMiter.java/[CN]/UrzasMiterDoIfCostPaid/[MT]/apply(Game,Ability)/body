{
  UrzasMiterWatcher watcher=(UrzasMiterWatcher)game.getState().getWatchers().get("UrzasMiterWatcher");
  if (!watcher.cards.contains(source.getFirstTarget()))   return super.apply(game,source);
  return false;
}
