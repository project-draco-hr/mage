{
  Target target=mode.getTargets().get(0);
  if (target.getNumberOfTargets() > 1)   return "tap " + target.getNumberOfTargets() + " target "+ mode.getTargets().get(0).getTargetName()+ "s";
 else   return "tap target " + mode.getTargets().get(0).getTargetName();
}
