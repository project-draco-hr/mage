{
  for (  UUID target : targetPointer.getTargets(source)) {
    Permanent permanent=game.getPermanent(target);
    if (permanent != null) {
      permanent.tap(game);
    }
 else {
      return false;
    }
  }
  return true;
}
