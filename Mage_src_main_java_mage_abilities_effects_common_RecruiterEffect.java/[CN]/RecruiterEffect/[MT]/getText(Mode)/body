{
  if (staticText != null && !staticText.isEmpty()) {
    return staticText;
  }
  return "search your library for any number of " + filter.getMessage() + " and reveal those cards. Shuffle your library, then put them on top of it in any order.";
}
